workflow:
    rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
      when: always
    - if: '$CI_COMMIT_BRANCH == "experiment_CICD_MAS"'
      when: always
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: always 

variables:
  test_var = $test_var

stages:   
      - test

# before_script:
#       - echo "Before script"
#       # Pull repositories
#       - cd C:\sandbox
#       - git checkout experiment_CICD_MAS
#       - git "pull
      
test_one:    
      stage: test
      tags:
      - MAS
      script:     
      - echo test_var
      - echo "Test cicd in sandbox"
      - python "a_failing_test.py"
      # - Rscript -e 'stopifnot(all(sapply(devtools::check(), length) == 0L))'
